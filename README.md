# Zendesk SSO JWT PHP example

Install dependencies

    composer install
    
Run local server

    php -S localhost:8000
    
Go to http://localhost:8000

A new user has been created in your Zendesk domain
